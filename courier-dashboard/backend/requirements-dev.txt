# Инструменты для тестов. Установка: pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.*
playwright==1.*
# После установки playwright один раз выполнить: python -m playwright install chromium
